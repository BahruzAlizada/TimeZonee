@model List<ShopMethod>

@foreach(var item in Model)
{
    <div class="col-xl-4 col-lg-4 col-md-6">
        <div class="single-method mb-40">
            @Html.Raw(item.Icon)
            <h6>@item.Title</h6>
            <p>@item.Description</p>
        </div>
    </div>
 
}